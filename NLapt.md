# NLapt（Natural Language Annotation Processing Tools）功能需求说明书

版本 v1.0 · 2026-07-11
定位：面向图像生成模型（LoRA / 微调）训练数据集的图文标注编辑工具

---

## 1. 产品概述

### 1.1 背景与定位

本软件用于编辑图像生成模型训练数据集中的 caption 文件。数据集采用 kohya 约定格式：每张图片配一个同名 `.txt` 文件存放描述文本（如 `img001.jpg` + `img001.txt`）。caption 内容为混合风格——danbooru 标签（逗号分隔的短标签）与自然语言句子并存于同一文件。

软件的核心定位是"人机协作的审核编辑台"：LLM 负责批量生成与改写，人负责快速验证、修正与确认。设计目标按优先级排序为：编辑速度（键盘流操作）、数据安全（任何批量操作可预览、可回滚）、LLM 深度集成（文本模型与视觉模型并用）。

### 1.2 目标用户与典型场景

单机使用的数据集制作者。三个典型工作流贯穿全部功能设计：其一是**从零打标**——导入一批无标注图片，用视觉模型批量生成初稿，再逐张人工审核确认；其二是**风格统一**——已有标注但风格杂乱，用 AI 批量重写为统一风格，通过 Diff 审核逐条把关；其三是**精修与维护**——逐张微调文本、管理触发词、全局查找替换、检查漏标。

### 1.3 术语约定

| 术语 | 含义 |
|---|---|
| caption | 与图片同名的 `.txt` 文件中的描述文本 |
| 标签 / tag | caption 中以逗号分隔的短片段（danbooru 风格） |
| 胶囊 / Chip | 标签在界面上的可交互渲染形态 |
| 触发词 / trigger word | 标识训练概念的固定前缀标签（如 `minahamu`） |
| 待审核建议 / pending | LLM 产出的、尚未被人工接受的新文本，独立于正文存放 |
| 作用域 | 批量操作的目标范围：当前文件 / 多选 / 筛选结果 / 全部 |
| 快照 | 批量操作前对全部 txt 的自动 zip 备份 |

---

## 2. 数据与文件规范

### 2.1 数据集结构与配对规则

用户打开一个文件夹作为数据集根目录。程序扫描其中受支持的图片文件（jpg、jpeg、png、webp、bmp），按"同目录、同主文件名"规则配对 `.txt` 文件，扩展名匹配不区分大小写。支持递归扫描子文件夹以兼容 kohya 的 `10_conceptname` 重复次数目录结构，列表中以相对路径区分不同目录下的同名文件。

配对结果分三类处理：完整配对（图 + txt）正常载入；只有图片没有 txt 的判定为"未标注"，在界面上明确标示，首次保存内容时自动创建同名 txt；只有 txt 没有图片的"孤儿文件"默认忽略不展示，设置中可开启"启动时提示清理孤儿 txt"。

### 2.2 文本编码与格式

统一以 UTF-8（无 BOM）读写，换行符统一为 `\n`。读取时对 GBK 等常见编码做自动检测，检测到非 UTF-8 时提示用户并在确认后转换保存。caption 语法层面不做强约束——解析器必须同时容纳纯标签序列、纯自然语言段落，以及两者混排（典型形态为开头若干标签、其后接一到多句描述）。

### 2.3 保存策略

自动保存的触发时机为：切换到另一张图片时、编辑框失去焦点时、任何工具操作应用后、程序退出前。写入采用原子方式——先写临时文件再 rename 覆盖原文件，避免写入中断导致 txt 损坏。底部状态栏常驻保存状态指示，呈现"已保存 / 保存中 / 保存失败（附原因）"三态。

### 2.4 备份与快照

任何影响多个文件的批量操作在执行前，自动将数据集内全部 txt 打包为一个 zip 快照，存入数据集根目录下的 `.backups/`，文件名包含时间戳与操作名（如 `2026-07-11_1430_批量替换.zip`）。默认保留最近 20 份，超出自动删除最旧的。提供"从快照恢复"入口：选择某份快照后先展示将被还原的文件数量，确认后整体还原，恢复动作本身也生成一份恢复前快照，保证可以反悔。

---

## 3. 界面总体布局

### 3.1 三栏结构

主界面为三栏式，配顶栏与底部状态栏：

```
┌──────────────────────────────────────────────────────────────┐
│ 顶栏：数据集路径 │ 进度统计（已确认 x / 总数 y）│ 主题 │ 设置    │
├───────────┬──────────────────────────────┬───────────────────┤
│ 图片列表   │         图片预览区            │     工具面板       │
│ ─────────  │  （自适应缩放 / 滚轮缩放平移）  │  ┌──────────────┐ │
│ 搜索框     │                              │  │ 查找 / 替换   │ │
│ 状态筛选   ├──────────────────────────────┤  │ 前缀 / 后缀   │ │
│ 缩略图     │ [AI 建议 Diff 审核条，有则显]  │  │ 翻译          │ │
│ +文件名    ├──────────────────────────────┤  │ AI 润色/重写  │ │
│ +状态点    │ Caption 编辑器               │  │ 历史记录      │ │
│ +token 数 │ 页签：胶囊 │ 逐句 │ 自由      │  └──────────────┘ │
│           │ 底部：字数 token 确认按钮      │                   │
├───────────┴──────────────────────────────┴───────────────────┤
│ 状态栏：保存状态 │ 当前文件路径 │ 快捷键提示                     │
└──────────────────────────────────────────────────────────────┘
```

左栏默认宽约 280px，右栏约 340px，两条分隔线均可拖拽调节宽度，左右面板可折叠以扩大预览区。窗口最小尺寸 1200 × 720。

### 3.2 界面语言

采用中文为主、行业术语保留英文的混排风格（如"胶囊 Chips""待审核 Pending""token"）。全部界面文案集中在语言资源文件中管理，为将来提供纯中文或纯英文切换保留结构。

### 3.3 主题系统

内置五套预设主题：浅色（白底）、纸感（暖米白）、浅灰、石墨（深灰蓝）、暗黑（近黑），默认使用浅色。自定义项包括强调色（取色器自由选择）、界面字号（小 / 中 / 大三档）、编辑区字体（默认界面字体，可切换等宽字体）。

实现上以 design token（CSS 变量或框架内等价机制）承载全部色彩，界面任何位置不得硬编码颜色，保证换肤完整覆盖。深色主题需同步处理原生控件配色，图片预览区在深色下使用中性深灰背景，透明 PNG 以棋盘格底纹呈现。主题选择与自定义值持久化到本地配置文件。

---

## 4. 左侧：图片列表面板

列表项由缩略图（约 56px）、文件名（等宽字体，超长中部省略）、状态点、token 数四个元素组成，当前选中项以强调色竖条与底色标识。缩略图由后台线程生成并缓存到本地缓存目录，首次打开大数据集时渐进式填充，不阻塞界面操作。

### 4.1 状态体系

| 状态 | 含义 | 进入条件 |
|---|---|---|
| 未标注 | 无 txt 或内容为空 | 扫描时判定，或内容被清空 |
| 草稿 | 有内容，但未经人工确认 | 任何人工编辑动作 |
| 待审核 | 存在未处理的 AI 建议 | LLM 操作产出建议后 |
| 已确认 | 人工审核通过 | 点击确认按钮或 Ctrl+Enter |

补充规则：已确认的文件再次被编辑后自动退回"草稿"（设置中可关闭该规则）；AI 建议与正文互不覆盖，建议文本独立存放于会话数据中，被接受后才写入 txt；"待审核"状态与正文自身的草稿或确认态并存，图标叠加显示。

### 4.2 筛选、搜索与排序

面板顶部为状态分段筛选控件，每个状态旁显示实时计数。搜索框支持按文件名或 caption 内容匹配；关键词前加 `-` 表示"不包含"，例如输入 `-1girl` 即筛出所有 caption 中不含 `1girl` 的图片，用于漏标排查。搜索基于程序启动时建立的内存全文索引，输入即时响应。排序方式支持文件名（默认，自然排序）、修改时间、token 数三种。

### 4.3 多选

支持 Ctrl 点选与 Shift 范围选择。存在多选时列表顶部显示"已选 n 项"与清除按钮，选中集自动成为右侧所有批量工具可选的作用域之一。

---

## 5. 中央：图片预览区

默认按可用区域适配缩放；鼠标滚轮在 10%–800% 间缩放，按住拖拽平移，双击在适配与 100% 原始尺寸间切换。预览区角标显示文件名、分辨率、格式与文件大小。左右两侧悬浮上一张 / 下一张按钮，与 ←/→ 快捷键等效。

加载策略：当前图片全尺寸解码，相邻前后各 2 张预解码，其余仅保留缩略图，保证连续快速切图时的响应速度。

---

## 6. 中央：Caption 编辑器（核心模块）

编辑器提供同一份文本的三种视图，通过页签切换（快捷键 1 / 2 / 3），任一模式下的修改实时同步到其余模式。编辑器底部常驻：字符数、估算 token 数、"标为已确认"按钮、撤销 / 重做按钮、自动保存指示。

### 6.1 胶囊模式（Chips）

**切分规则**：按半角与全角逗号切分文本为片段，每个片段渲染为一枚可交互胶囊。含句末标点的片段判定为"长句胶囊"，以更宽的浅底样式与短标签胶囊区分，两类胶囊交互行为一致。

**交互行为**：单击胶囊原位转为行内输入框（宽度随内容自适应），Enter 确认、Esc 取消；确认时若输入内容含逗号，自动拆分为多枚胶囊。悬停胶囊显示删除按钮，点击即删。胶囊支持拖拽排序，释放后按新顺序重组文本。列表末尾常驻"+ 添加"入口。右键胶囊弹出菜单：复制、删除、"统计该标签在全部文件中的出现次数"——统计结果直接显示在菜单中，点击结果可跳转为列表搜索筛选。

**重组与去重**：写回文本时以 `", "` 连接全部片段，空片段自动清除。检测到与其他胶囊完全重复的标签时在其角标提示，并提供"一键去重"操作。

**空状态**：内容为空时显示引导文案与两个快捷入口——"添加第一个标签"进入新增输入；"用 AI 生成"跳转 AI 面板并预选当前文件与视觉模式，走看图初标流程。

### 6.2 逐句模式（Sentences）

按句末标点（`.` `!` `?` `。` `！` `？`）切分，每句占一行独立编辑框，高度随内容自动增长。v1 不实现复杂分句算法，小数点与缩写可能产生误切，作为已知限制在文档与界面提示中注明，用户可用"与上一句合并"操作手动修正。

每行右侧操作组包含：上移、下移、删除、与上一句合并、"AI 改写此句"。其中 AI 改写将该句连同全文作为上下文发送给模型，返回后原位替换该句并计入撤销历史。底部提供"新增一句"。重组写回时英文句之间以空格连接，中文句号结尾的句子之间直接连接。

### 6.3 自由文本模式（Free）

完整多行文本框，承载不适合结构化编辑的场景。选中任意片段时，编辑框上沿浮现操作条，显示"已选中 n 字符"并提供三个操作：**替换为**（内联输入框 + 确认按钮，替换选区）；**AI 改写选中**（仅将选区连同上下文发给模型，返回后替换选区）；**翻译选中**（译文显示在操作条下方浮层，只读可复制）。

与查找工具联动：查找命中的片段在本模式中高亮显示，"下一处"操作滚动定位到对应位置。撤销粒度：连续键入以约 600ms 停顿分组，每组作为一步进入撤销栈。

### 6.4 确认操作与审核节奏

"标为已确认"（Ctrl+Enter）将当前文件置为已确认，并自动跳转到列表中下一个非已确认文件——这是逐张人工审核的核心节奏，配合状态筛选可实现"只过一遍未确认的"。

### 6.5 token 估算

以 CLIP tokenizer 规则估算当前 caption 的 token 数，显示为"≈ n tokens"。超过 77 时数值转为警示色，悬停提示"超出 CLIP 77 上限，SDXL 文本编码将截断；Flux / T5 类编码器可放宽"，仅警示不阻止编辑。

---

## 7. 右侧：工具面板

五个页签。所有具备批量能力的工具复用统一的**作用域选择器**（详见第 9 章）：当前文件 / 已多选（n）/ 当前筛选结果（n）/ 全部文件（n），括号内实时显示对应文件数。

### 7.1 查找 / 替换

字段为"查找内容"与"替换为"，选项包括区分大小写、全词匹配、正则表达式；正则语法错误时即时提示并禁用执行按钮。输入查找内容后实时显示两级命中统计："当前文件 n 处 · 作用域内 m 处（涉及 k 个文件）"。

提供三种执行方式。**逐个替换**：在当前文件内定位并高亮下一处命中（自由模式联动滚动），确认替换后跳到下一处。**替换当前文件全部**：一次性处理当前文件。**应用到作用域**：执行前弹出预览面板，逐文件列出每处命中的上下文摘录（前后各约 20 字符），允许勾选排除个别文件；确认后先自动生成快照再执行，完成时以摘要提示"已在 k 个文件中替换 m 处"并写入操作日志。

### 7.2 前缀 / 后缀

前缀与后缀两个输入框可独立启用。选项包括：目标文本已以该前缀开头（或该后缀结尾）时跳过，默认开启，防止触发词被重复叠加；连接符可选 `", "`、空格或无。

内置**触发词预设**区：用户可保存常用触发词（如 `minahamu`），每个预设提供一对操作——"批量添加到开头"与"批量从所有文件移除"，移除按精确匹配前缀文本（含连接符）执行。作用域选择、执行前预览与快照规则与 7.1 相同。

### 7.3 翻译（中英对照）

面板为上下两栏对照：上栏为当前 caption 原文的只读镜像，随编辑实时更新；下栏为译文，只读并附复制按钮。翻译方向自动判定（原文以英文为主则译为中文，反之亦然），可手动切换。翻译经由已配置的 LLM 文本模型执行，提示词模板可在设置中编辑；未配置 API 时本页签呈现引导态，指向设置页。

译文带时间戳缓存；翻译后原文再被修改时，译文区标记"原文已更新"并提供重新翻译按钮。**批量翻译**对作用域内全部文件预生成译文缓存（带进度、可取消），便于逐张审核时即看即读。译文仅作辅助阅读，不写入 txt；如确需以译文替换正文，提供带二次确认的显式按钮，并提示训练数据通常应保留英文。

### 7.4 AI 润色 / 重写

本页签是 LLM 能力的主入口。

**操作类型**（单选）：润色（修正语法与流畅度，尽量保留原始信息量）、重写（重组结构、统一表达风格）、扩写（补充构图、光线、氛围等维度的描述）、精简（压缩至目标 token 数，目标值可填写）、自定义指令。每种类型对应一个可编辑的提示词模板，模板支持变量插值：`{caption}` 当前文本、`{filename}` 文件名、`{trigger}` 已配置触发词。自定义指令可保存为命名模板供复用。

**视觉开关**："随请求发送图片"。开启后请求携带图片（按设置压缩至最长边 1024px 后编码），实现看图修订；对"未标注"文件即为看图生成初标。关闭时为纯文本操作，自动改走配置中的文本模型以降低成本。

**试跑**：从作用域内随机抽取 N 条（默认 5）执行，逐条展示"原文 → 结果"对照，用于全量执行前验证提示词效果。试跑结果可直接采纳为这几条的待审核建议，也可仅作参考丢弃。

**执行与结果**：单文件执行的结果直接进入该文件的待审核建议并展开 Diff 审核条；批量执行走第 9 章的批量流程，全部结果写入各文件的待审核建议。任何情况下 AI 结果都不直接覆盖正文。

**输出约束与清洗**：提示词模板末尾固定拼接输出约束（仅输出最终 caption 文本，不得包含解释、引号或代码块）；返回内容做统一清洗——剥离首尾引号、Markdown 代码围栏、"Caption:" 之类前缀。清洗后为空、请求超时或接口报错的文件进入失败列表。

### 7.5 历史记录 / 撤销

**每文件撤销栈**：快照式实现，每个文件保留最近 50 步。Ctrl+Z 与 Ctrl+Shift+Z 作用于当前文件；焦点位于文本框内时优先交给输入框的原生撤销，焦点在外则走应用级栈，两者通过 6.3 的输入分组规则衔接为一致的体验。

**全局操作日志**：按时间倒序列出本次会话的全部操作，每条包含时间、操作描述与影响范围（如"批量替换 `girl` → `woman` · 9 个文件 17 处""AI 重写（视觉）· 产生待审核 32 条"）。批量类条目提供**整体回滚**：基于该操作执行前的自动快照还原全部受影响文件，回滚动作本身也记入日志。日志为会话级；跨会话的恢复能力由 2.4 的快照体系承担，日志页底部提供"打开快照管理"入口。

---

## 8. 设置：LLM 与 API 配置

支持配置多个服务档案，每档包含：接口类型（OpenAI 兼容 / Anthropic / Ollama 本地）、base URL、API key（仅存本机配置文件，界面掩码显示）、文本模型名、视觉模型名、temperature、max_tokens、系统提示词。文本与视觉模型允许不同，纯文本操作自动使用文本模型以控制成本。提供"连接测试"按钮，发送最小请求验证连通性与鉴权。

请求控制参数：并发数（默认 4）、请求最小间隔、超时时间、失败重试次数（默认 2 次，指数退避）。图片压缩上限边长默认 1024px，可调。

隐私边界：除用户显式触发的 LLM 请求外，程序零联网；图片数据仅在视觉开关开启的请求中外发。该边界在设置页以显著文案说明。

---

## 9. 批量处理引擎（公共机制）

所有批量能力（替换、前后缀、翻译、AI 操作、去重等）复用同一条流程：

选定作用域 → 影响预览（文件数与必要的逐条预览）→ 确认 → 自动快照 → 执行 → 结果汇总。

执行期界面提供：进度指示（n / m）、当前处理文件名、暂停 / 继续 / 取消（取消时保留已完成部分）、实时失败计数。LLM 类批量任务记录已完成文件清单，任务中断（含程序崩溃）后再次发起相同操作时提示"从断点继续"。执行完成后弹出成功 / 失败摘要，失败项汇总于结果面板，支持单独重试或导出失败清单。全部批量操作写入操作日志并可整体回滚。

可选增强（P2）：LLM 批量执行前，基于提示词模板长度与作用域内 caption 平均长度估算 token 消耗，在确认对话框中展示大致费用。

---

## 10. 审核工作流

**状态机**：未标注 →（AI 生成 / 人工编辑）→ 草稿或待审核 → 已确认。状态定义见 4.1。

**建议条与 Diff 视图**：存在 AI 建议的文件，在预览区与编辑器之间显示醒目的建议条，内嵌词级 Diff——原文与建议合并渲染，被删除的片段以红底删除线呈现，新增片段以绿底呈现。建议条提供三个操作：**接受**（建议写入正文并落盘，清除建议）、**拒绝**（丢弃建议）、**编辑后接受**（建议载入编辑器成为正文草稿，清除建议标记，由用户修改后自行确认）。

**批量审核节奏**：左栏筛选"待审核"后逐张查看 Diff，快捷键 A 接受、X 拒绝，操作后自动跳转下一条待审核——这是与 LLM 批量处理配套的核心人审动线。建议条上另提供"全部接受 / 全部拒绝"，作用于当前筛选范围，需二次确认并自动快照。

---

## 11. 键盘快捷键

| 快捷键 | 功能 |
|---|---|
| ← / → 或 J / K | 上一张 / 下一张图片 |
| 1 / 2 / 3 | 切换胶囊 / 逐句 / 自由编辑模式 |
| Ctrl+Enter | 标为已确认并跳转下一个未确认文件 |
| A / X | 接受 / 拒绝当前 AI 建议（存在建议时） |
| Ctrl+F | 聚焦查找输入框 |
| Ctrl+Z / Ctrl+Shift+Z | 撤销 / 重做（当前文件） |
| Ctrl+S | 立即保存 |
| Ctrl+, | 打开设置 |
| Esc | 退出行内编辑 / 关闭浮层 |

冲突规避规则：焦点位于任何文本输入控件时，字母与数字类快捷键（J / K / A / X / 1 / 2 / 3）不生效；方向键在预览区聚焦时用于切图，在文本框内保持光标移动语义。

---

## 12. 非功能需求

**性能**：以 10,000 张图片的数据集为基准——启动扫描与全文索引构建在后台进行，界面先行可用，索引完成不超过 10 秒；列表采用虚拟滚动保持流畅；切图响应不超过 100ms（依赖第 5 章预解码策略）。全部 txt 内容常驻内存（1 万条 × 2KB 约 20MB，可接受），保存按脏标记增量落盘。

**可靠性**：原子写入；程序异常退出后重启可恢复未落盘的草稿与未处理的待审核建议（依赖会话恢复文件）；快照体系作为最终兜底。

**隐私**：除 LLM 请求外零联网，所有数据与配置存于本机。

**平台**：桌面应用，Windows 优先，macOS 与 Linux 兼容为加分项。

---

## 13. 技术选型建议（非约束性）

推荐 Python + PySide6 桌面方案：Pillow 负责缩略图生成与图片压缩，openai / anthropic SDK 或 httpx 负责 API 调用，QThreadPool 承载批量并发任务，tiktoken 或开源 CLIP 分词器做 token 估算，PyInstaller 打包分发。若选择 Web 架构（FastAPI + 前端页面），键盘流体验与本地文件读写授权需要额外投入，v1 不推荐。

---

## 14. 版本规划

| 阶段 | 范围 | 验收基准 |
|---|---|---|
| P0（最小可用闭环） | 第 2 章全部；三栏布局（主题仅浅色与暗黑两套）；图片列表含状态、筛选、搜索；图片预览；自由文本模式 + 确认操作 + token 估算；查找替换（含批量、预览、快照）；前缀后缀；批量引擎（预览、快照、进度）；核心快捷键 | 能完整走通"打开文件夹 → 编辑 → 批量替换 → 确认 → 安全落盘"，批量误操作可从快照恢复 |
| P1（完整体验） | 胶囊模式；逐句模式；AI 润色/重写全量（含视觉初标、试跑、模板变量）；待审核状态与 Diff 审核流；历史记录与整体回滚；翻译；五套主题与自定义强调色 | 能完整走通"视觉模型批量初标 → 逐条 Diff 审核（A/X 键盘流）→ 全部确认" |
| P2（增强项） | 费用预估；断点续跑细化；标签出现次数统计与跳转筛选；查找替换与指令模板的预设管理；快照管理界面；孤儿 txt 清理 | 按需排期 |

