"""Core building blocks: errors, events, states, models, config.

Import submodules directly, e.g. ``from nlapt.core.errors import NLaptError``.
This package init stays empty to keep import order trivial for all modules.
"""

from __future__ import annotations
